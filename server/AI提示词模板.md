# AI 提示词模板

## 后端功能开发

**功能描述**: [添加项目管理功能]

- 在 `domain/` 定义 Project 实体和 ProjectRepository 接口
- 在 `ent/schema/` 创建数据库 schema
- 在 `repository/` 实现 ProjectRepository 接口
- 在 `usecase/` 实现业务逻辑
- 在 `api/handler/` 定义 API 函数
- 在 `api/route/` 添加路由

## 前端功能开发

**功能描述**: [添加项目管理功能]

- 在 `web/src/features/project/` 创建功能模块
- 在 `web/src/services/` 创建 API 调用
- 创建 React 组件和 hooks
- 在 `web/src/routes/` 配置路由